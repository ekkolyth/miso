{
  "bin": {
    "miso": "./cli.mjs"
  },
  "description": "the agnostic package manager",
  "files": [
    "cli.mjs",
    "miso-darwin-amd64",
    "miso-darwin-arm64",
    "miso-linux-amd64",
    "miso-linux-arm64"
  ],
  "license": "MIT",
  "name": "@ekkolyth/miso",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ekkolyth/miso.git"
  },
  "scripts": {
    "prepublishOnly": "mkdir -p bin && GOOS=darwin GOARCH=amd64 go build -o bin/miso-darwin-amd64 ./cmd && GOOS=darwin GOARCH=arm64 go build -o bin/miso-darwin-arm64 ./cmd && GOOS=linux GOARCH=amd64 go build -o bin/miso-linux-amd64 ./cmd && GOOS=linux GOARCH=arm64 go build -o bin/miso-linux-arm64 ./cmd && cp bin/miso-darwin-amd64 . && cp bin/miso-darwin-arm64 . && cp bin/miso-linux-amd64 . && cp bin/miso-linux-arm64 ."
  },
  "version": "0.1.15"
}
