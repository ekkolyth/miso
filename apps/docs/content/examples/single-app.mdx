---
title: Single App
---

# Single App Example

A standard single-app `miso.json` — TUI, env validation, and flags, with a `dev` task that brings the app up alongside `convex dev` (declared as a `convex` script).

<ConfigExample name="single" />
